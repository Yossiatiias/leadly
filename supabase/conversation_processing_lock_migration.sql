-- ─── נעילת עיבוד לשיחה — מונע שתי קריאות מקבילות לבוט על אותה שיחה ─────────
-- שורש באג שנתפס בפועל: לקוח ששלח שתי הודעות תוך פחות משנייה גרם לשתי
-- קריאות ai-respond מקבילות על אותה שיחה, שתיהן עברו את בדיקת ה-rate-limit
-- (כי אף אחת מהן עדיין לא הספיקה לשלוח תשובה), וכל אחת קבעה תור/רופא בנפרד —
-- הלקוח קיבל שתי הודעות שונות, והתור שנשמר בפועל נשאר בלי רופא משויך כלל.
ALTER TABLE conversations ADD COLUMN IF NOT EXISTS ai_processing_started_at TIMESTAMPTZ;
